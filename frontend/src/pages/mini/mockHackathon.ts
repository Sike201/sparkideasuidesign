/**
 * Deprecated — the mock hackathon that was prepended to `/m/hackathons` and
 * short-circuited `/m/hackathons/:id` has been removed. The mini-app now
 * shows only the real backend hackathons. File kept as an empty module to
 * avoid breaking any stale imports during the transition; delete once
 * you've pulled and confirmed nothing imports from here.
 */

export {}
